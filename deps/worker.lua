--[[
  Worker layer of phage
]]

local Object = require('coer').Object

local type, pairs, next =
      type, pairs, next

local _M = Object:extend()

function _M:initialize(taskmap)
end




















-----------------------------------------------------
return _M